0. Ensure that the Docker server is running.  Check it via docker ps.
1. For Development:
   a. The Docker compose file is docker-compose-v1.yml
   b. To run it and launch it: docker-compose -f docker-compose-v1.yml up -d

